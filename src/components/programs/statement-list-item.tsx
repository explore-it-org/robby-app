import { Statement } from '@/programs/statements';
import { Text } from 'react-native';

interface Props {
  statement: Statement;
}

export function StatementListItem({ statement }: Props) {
  return <Text>Placeholder</Text>;
}
